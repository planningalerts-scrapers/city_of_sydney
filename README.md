Development applications for PlanningAlerts.

## Configuration

Set `MORPH_AUSTRALIAN_PROXY` to an Australian proxy, preferably one that
rotates its IP address. This is required on morph.io because the council's
website (behind Cloudflare) blocks requests from non-Australian IP addresses.
